---
description: 福泰會員優惠活動 — 環境變數與 Secrets 管理規範
alwaysApply: true
---

# 環境變數管理規範

## 權威來源
- 運行時 Secrets：**GitHub Actions Environments**（`staging` / `production`）
- 清單紀錄：**docs/ENV-MANIFEST.txt**（只記名稱與用途，不含密碼值）

## 新增或修改環境變數時，必須同步
1. `docs/ENV-MANIFEST.txt` — 新增列並更新 staging/production 勾選狀態
2. GitHub Environment Secrets — 設定實際值
3. `.github/workflows/deploy-staging.yml`（及 production workflow）— `--set-env-vars` 注入
4. `server-api/app/config.py` — 若為應用讀取的變數，加入設定欄位

## 禁止
- 將 secret 值寫入 `.py`、HTML、README、或任何會 commit 的檔案
- 將 `.env` 檔 commit 至 Git
- 在程式碼中 hardcode LINE token、Sheet ID、GCP 金鑰

## 測試 vs 正式
- 測試 GCP：`kenzy@dd99.games` 既有 Project，不另開新 Project
- 正式上線：依 ENV-MANIFEST 遷移 checklist 逐項重建

## 專案邊界
- 與接駁車系統（Shuttle-system-github）完全獨立：不共用 API、Sheet、Service Account
